import { StyleSheet } from 'react-native'; 

const styles = StyleSheet.create({
    texto: {
        color: '#00f', 
        textAlign: 'center', 
        padding: 10, 
    }, 
});

export default styles;